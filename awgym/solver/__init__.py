"""awgym.solver — the Phase 2 solver league (the six-role dream-team
protocol reimplemented as ONE leader-switched agent, with LeWM as the
simulator). The leader loop, workspace, DECISION mirror and findings card
live here; the role toolpacks live in Library/packs/awgym/roles/."""
